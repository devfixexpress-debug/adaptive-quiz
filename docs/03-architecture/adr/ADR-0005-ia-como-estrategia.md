# ADR-0005 — IA como estrategia adicional

**Estado:** Aceptado / implementación futura

## Decisión
La IA implementará el mismo contrato `AdaptationStrategy`. No se invocará desde entidades ni
desde UI.

## Resultado
`RuleBasedAdaptationStrategy` seguirá disponible como fallback y como versión certificable.
